/* pages/music-player/music-player.wxss */
.bg-image,
.cover {
  position: fixed;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  z-index: -2;
}

.cover {
  background-color: rgba(0, 0, 0, .3);
  backdrop-filter: blur(13px);
}